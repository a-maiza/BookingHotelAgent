# Template only — real value is injected by CI via kubectl create secret.
# Do NOT commit real passwords here.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: booking-agent
type: Opaque
stringData:
  POSTGRES_PASSWORD: REPLACE_ME
